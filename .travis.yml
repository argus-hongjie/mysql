language: java


services:
  - docker
  
jdk: oraclejdk8

before_install:
  - sudo /etc/init.d/mysql stop
  - mysql -u root -p mysql --version
  - mysqladmin ping -h127.0.0.1 -P3306

script:
  mvn clean test